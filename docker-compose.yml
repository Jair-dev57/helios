services:
  db:
    image: postgres:16
    container_name: helios_db
    environment:
      POSTGRES_USER: helios
      POSTGRES_PASSWORD: helios123
      POSTGRES_DB: helios
    ports:
      - "5434:5432"
    volumes:
      - helios_data:/var/lib/postgresql/data

volumes:
  helios_data: